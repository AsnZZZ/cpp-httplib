repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v23.1.0  # CIのstyle-checkと同じバージョンに揃える
    hooks:
      - id: clang-format
        files: ^(httplib\.h|(example|fuzzing|test)/[^/]+\.(cpp|cc|h))$
        args: [-i]  # インプレースで修正